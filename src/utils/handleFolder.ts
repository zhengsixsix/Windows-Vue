import useFolderStore from "@/store/folderStore";
const store = useFolderStore();
export const searchTargetFolderByPath = (path: string) => {
  if (path.length === 1) {
  }
  const desc = store.storeCompletedFolder;
};
